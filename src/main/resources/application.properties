spring.datasource.url=jdbc:postgresql://onlinestore.c7kkevaszpkn.eu-west-1.rds.amazonaws.com:5432/onlinestore
spring.datasource.username=root777
spring.datasource.password=dbpass1234X

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

base.photos.directory.path=./photos/

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=1MB
# Max file size.
spring.servlet.multipart.max-file-size=2MB
# Max Request Size
spring.servlet.multipart.max-request-size=5MB
